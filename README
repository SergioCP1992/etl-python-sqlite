
"""
Proyecto 3 — Pipeline ETL (Python → SQLite)

Este proyecto realiza un pipeline ETL sencillo:
- Extract: lee CSV desde la carpeta /data
- Transform: normaliza correos, valida precios y calcula el total de cada pedido
- Load: guarda los datos limpios en database/ecommerce.db

Estructura del proyecto:
etl-python-sqlite-min/
│── etl.py
│── requirements.txt
│── data/ (CSV)
└── database/ (SQLite)

Ejecutar:
    python etl.py

Verificar BD (opcional, si tienes sqlite3):
    sqlite3 database/ecommerce.db
    .tables
    SELECT * FROM pedidos;
"""
